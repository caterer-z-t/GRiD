# === Genomic data files ===
*.fa
*.fai
*.bam
*.bai
*.cram
*.crai
*.haps
*.haps.gz
*.sample
*.map
*.ped
*.match
*.bed
*.bim
*.fam
*.vcf
*.vcf.gz
*.vcf.gz.tbi
*.vcf.bgz
*.vcf.bgz.tbi
*.tsv
*.ucsc_bed
test/files

*hg38*

# === Log files ===
*.log

# === HPC / SLURM outputs ===
slurm/

# === Cromwell workflow outputs ===
cromwell-executions/
cromwell-workflow-logs/

# === Cromwell Java Container ===
cromwell-*.jar

# === Python cache files ===
__pycache__/
.pytest_cache/

# === Distribution / build files ===
GRiD.egg-info/

# === readthedocs build files ==
docs/build/

# === Mukamel code files ===
mukamel/

# SOL / JHS / MESA / ARIC / NWD files
files/*{SOL,JHS,MESA,ARIC,NWD,nwd}*
*SOL*
*JHS*
*MESA*
*ARIC**
*NWD*
*nwd*
files/diploid_calls.txt

# Playground files + directories
playground/

# === Ignore R history files ===
.Rhistory
docs/source/paper_sphinx.md

# === Ignore claude files ===
.claude/

# === Ignore JOSS paper files ===
paper/*
jats/*

# docker run --rm \
#     --platform linux/amd64 \
#     --volume $PWD:/data \
#     --user $(id -u):$(id -g) \
#     --env JOURNAL=joss \
#     openjournals/inara

# === Ignore coverage files ===
.coverage

# === Ignore Archive files ===
GRiD.zip
# git archive --format=zip --output=GRiD.zip HEAD
